---
import OptionCard from "./OptionCard.astro"
import { options } from "../../data/options"
---

<section class="grid grid-cols-2 gap-4 m-4">
  {options.map(option => (
    <OptionCard {...option} />
  ))}
</section>
